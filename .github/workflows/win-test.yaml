name: windows-test

on: 
    workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Running on Windows"
      - run: node --version
